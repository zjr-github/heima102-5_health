<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.health.dao.MenuDao">
    <select id="findByUserId" parameterType="int" resultType="menu">
        SELECT 
        m.id,m.name title,m.path,m.linkUrl,m.icon,m.parentMenuId
        from 
        t_menu m,
        t_role_menu rm,
        t_role r,
        t_user_role ur,
        t_user u 
        WHERE u.id = ur.user_id and ur.role_id = r.id and
        r.id = rm.role_id and rm.menu_id =m.id and u.id =#{id};
    </select>
</mapper>